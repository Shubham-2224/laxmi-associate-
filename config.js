/**
 * OPTIONAL — for instant email without FormSubmit activation:
 * 1. Open https://web3forms.com
 * 2. Enter Laxmiassociates2005@gmail.com → Create Access Key
 * 3. Paste key below (replace YOUR_ACCESS_KEY_HERE)
 *
 * If left as default, form uses FormSubmit (activate once via email from FormSubmit).
 */
window.EMAIL_CONFIG = {
  web3formsKey: 'YOUR_ACCESS_KEY_HERE'
};
